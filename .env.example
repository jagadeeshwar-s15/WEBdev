# Public base URL of the deployed site (no trailing slash).
# Used for canonical URLs, Open Graph tags, sitemap.xml and robots.txt.
# During local development this can be left unset — it falls back to a
# placeholder in lib/site.ts.
NEXT_PUBLIC_SITE_URL=https://www.deekshithadentalclinic.com
